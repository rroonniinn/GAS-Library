/**
 * Kombinator id jest funkcją zwracającą tę samą wartość,
 * którą otrzymała jako argument.
 *
 * @param {any} a Przekazana dowolna wartość
 * @returns {any} Wcześniej przekazana dowolna wartość
 */

const id = a => a;

export { id };
