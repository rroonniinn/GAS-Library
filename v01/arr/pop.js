/**
 * Zwraca ostatni element tablicy. Nie mutuje tablicy.
 * Dla pustej tablicy zwraca undefined
 *
 * @param {Array} arr Tablica
 * @returns {Any}
 */
const pop = arr => arr[arr.length - 1];

export { pop };
